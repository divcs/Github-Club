// Add this to a separate JS file or include it in the HTML <script> block
// GSAP animation for the BaseClub logo
gsap.from('.baseclub-logo', {
  x: -50,
  opacity: 0,
  duration: 1,
  ease: 'power2.out',
})
